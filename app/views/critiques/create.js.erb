<% Rails.logger.debug "IN CREATE JS" %>

$("#comments").prepend("<tr><%= escape_javascript(render(
                     partial: "critiques/critique",
                     layout: false,
                     locals: { critique: @critique } ))%></tr>");